### Particles

#### Direction

Determines what direction the front of the ladder faces.

`Range input (float)`
`Range: 0-3`
`Default: 0`

#### Style > Encoding

`Dropdown`
`Default: Default`
```
Options:
Default
sRGBEncoding
LinearEncoding
```

#### Style > Color

Base/diffuse color.

`Color input (hex)`
`Default: #FFFFFF`

#### Particles > Texture

`Dropdown`
`Default: Default`
```
Options:
Default
Grass
Fog
Ember
```

#### Particles > Blending

`Dropdown`
`Default: None`
```
Options:
None
Normal
Additive
Subtractive
Multiply
```

#### Particles > Count

`Range input (float)`
`Range: 0-300`
`Default: 0`

#### Particles > Size

`Range input (float)`
`Range: 0-30`
`Default: 0`

#### Particles > Scale

`Range input (float)`
`Range: -10-10`
`Default: 0`

#### Particles > Movement > Speed

`Range input (float)`
`Range: -10-10`
`Default: 0`

#### Particles > Movement > Deceleration

`Range input (float)`
`Range: -1-1`
`Default: 0`

#### Particles > Movement > Gravity

`Range input (float)`
`Range: -10-10`
`Default: 0`

#### Position

Location of the object in Cartesian coordinates. Positive Y is in the up direction. X and Z location defines the *center* and the Y location defines the bottom.

`Number input (float)`
`Default: 0, 0, 0`

#### Rotation

Rotation about each Cartesian axis. The Y rotation, for example, allows users to rotate an object laying on the XZ plane (a horizontal surface). Consider 1.3.4 and be mindful of using nonzero rotation with collidable objects.

`Number input (float)`
`Default: 0, 0, 0`

#### Size

Scale of the object.

`Number input (float)`
`Default: 0, 0, 0`

